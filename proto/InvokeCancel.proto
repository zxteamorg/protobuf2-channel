syntax = "proto2";
package org.zxteam.channel.v1;

import public "Type.proto";

message InvokeCancel /* extends Message */ {
  // 1, 2, 3 - reserved

  // Message
  required Type   messageType   = 4 [default = INVOKE_CANCEL];

  // InvokeCancel
  required uint32 invokeId      = 5;
}
